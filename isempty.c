#include "push_swap.h"

void	ag_isempty(t_data *data)
{
	if (data->stack_b == -1)
	{
		printf("empty stack B");
		// return (0);
	}
	else
	{
		printf("every thing ok and not empty");
	}
	// return (1);
}